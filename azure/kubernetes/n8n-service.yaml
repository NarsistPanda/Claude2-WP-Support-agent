apiVersion: v1
kind: Service
metadata:
  name: whatsapp-support-n8n
  namespace: whatsapp-support
  labels:
    app: whatsapp-support
    component: n8n
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5678
    protocol: TCP
    name: http
  selector:
    app: whatsapp-support
    component: n8n
